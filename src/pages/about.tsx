import React from 'react'

const about = () => {
  return (
    <div>
      <p>Hello from about page</p>
      <p>Demo for file based routing: <br/> no need to specify routing or path explicitly. It can take from file name</p>
    </div>
  )
}

export default about
